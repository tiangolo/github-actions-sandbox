name: Label Approved

on:
  schedule:
    - cron: "* * * * *"

jobs:
  label-approved:
    runs-on: ubuntu-latest
    steps:
    - uses: tiangolo/label-approved@main
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        debug: true
